# Agent rules - must follow

Personal art portfolio for Norwegian artist Elisabeth Fure Schwarz, built with Astro 7 + Tailwind CSS v4, content managed locally with Keystatic (JSON in `src/content/*`, images in `src/assets/artworks`), deployed to Vercel.

Write tests first where possible/reasonable (Playwright); otherwise verify work with Playwright or ask the user for feedback before declaring done.

Only run full test suite when I explicitly ask for it. When working on a feature, run only relevant tests for that specific feature, and make sure they pass.

Respond concisely, no fluff.
